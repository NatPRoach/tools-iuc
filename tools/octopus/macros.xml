<?xml version="1.0"?>
<macros>
    <token name="@TOOL_VERSION@">0.5.2b</token>
    <token name="@PROFILE@">20.01</token>
    <xml name="requirements">
        <requirements>
            <requirement type="package" version="@TOOL_VERSION@">octopus</requirement>
            <yield />
        </requirements>
    </xml>
    <xml name="version_command">
        <version_command>octopus --version</version_command>
    </xml>
    <xml name="citations">
        <citations>
            <citation type="bibtex">@online{octopus,
              author = {Dan Cooke},
              title = {octopus},
              year = 2021,
              url = {https://github.com/luntergroup/octopus},
              urldate = {2021-04-04}
            }</citation>
            <citation type="doi">10.1038/s41587-021-00861-3</citation>
        </citations>
    </xml>

    <!--
        command
    -->

    <token name="@REF_FASTA@"><![CDATA[
        #if $reference_source.reference_source_selector == 'history':
            #if $reference_source.ref_file.ext.endswith(".gz")
                gunzip -c '$reference_source.ref_file' > reference.fa &&
            #else
                ln -f -s '$reference_source.ref_file' reference.fa &&
            #end if
        #else:
            ln -f -s '$reference_source.ref_file.fields.path' reference.fa &&
        #end if
    ]]></token>

    <!--
        input
    -->
    <xml name="reference">
        <conditional name="reference_source">
            <param name="reference_source_selector" type="select" label="Choose the source for the reference genome">
                <option value="cached">Use a built-in genome</option>
                <option value="history">Use a genome from history</option>
            </param>
            <when value="cached">
                <param name="ref_file" type="select" label="Using reference genome" help="Select genome from the list">
                    <options from_data_table="all_fasta">
                        <filter type="sort_by" column="2"/>
                        <validator type="no_options" message="No reference genomes are available"/>
                    </options>
                    <validator type="no_options" message="A built-in reference genome is not available for the build associated with the selected input file"/>
                </param>
            </when>
            <when value="history">
                <param name="ref_file" type="data" format="fasta,fasta.gz" label="Use the following dataset as the reference sequence" help="You can upload a FASTA or FASTQ sequence to the history and use it as reference"/>
            </when>
        </conditional>
    </xml>

    <!--
        Help
    -->

    <token name="@WID@"><![CDATA[
    ]]></token>
    <token name="@REFERENCES@"><![CDATA[
    ]]></token>
</macros>